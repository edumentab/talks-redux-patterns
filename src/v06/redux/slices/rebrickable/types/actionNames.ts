/* REFAC|EDITCOMMENT
Since <span data-file-link="../../../lib/factory"><code>factory</code></span> now makes the creators return `guards` which the <span data-file-link="../reducer"><code>rebrickableReducer</code></span> uses, we no longer need to maintain this union of possible actions.

Similarly we delete <span data-file-link="../../guessingGame/types/actionNames"><code>GuessingGameActionNames</code></span> and <span data-file-link="../../ui/types/actionNames"><code>UIActionNames</code></span>.
*/
