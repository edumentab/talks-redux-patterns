/* REFAC|EDITCOMMENT
We split the tests for <span data-file-link="./reducer"><code>guessingGameReducer</code></span> into action-specific tests instead (<span data-file-link="./actions/setCurrentSet.test"><code>setCurrentSet.test</code></span> and <span data-file-link="./actions/setCurrentTheme.test"><code>setCurrentTheme.test</code></span>).

The same was done with <span data-file-link="../guessingGame/reducer.test"><code>guessingGameReducer.test</code></span> and <span data-file-link="../rebrickable/reducer.test"><code>rebrickableReducer.test</code></span>.
*/
test.todo('This line prevents Jest from barfing at empty test file')
