/* REFAC|EDITCOMMENT
Thunks have been thrown out in favour of <span data-file-link="../lib/types/consequence"><code>Consequences</code></span> and the <span data-file-link="../lib/consequence"><code>consequenceMiddleware</code></span>.

So instead of `AppThunks` we now have <span data-file-link="./appCons"><code>AppCons</code></span> instead!
*/
